#general {
  #lock_cmd = hyprlock
  #before_sleep_cmd = loginctl lock-session
  #after_sleep_cmd = hyprctl dispatch dpms on
#}

## Turn off display and lock after 15 minutes
#listener {
  #timeout = 900   # 15 min
  #on-timeout = hyprlock && hyprctl dispatch dpms off
  #on-resume = hyprctl dispatch dpms on
#}

## Suspend after 20 minutes
#listener {
  #timeout = 1200  # 20 min
  #on-timeout = loginctl lock-session && systemctl suspend
  #on-resume = hyprctl dispatch dpms on
#}


listener {
  timeout = 900  # 15 min
  on-timeout = hyprctl dispatch dpms off
  on-resume = hyprctl dispatch dpms on
}
listener {
  timeout = 1200 # 20 min
  on-timeout = systemctl suspend
  on-resume = hyprctl dispatch dpms on
}
